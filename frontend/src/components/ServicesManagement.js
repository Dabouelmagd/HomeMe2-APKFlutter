// Update the import to include formatDate from dateUtils
import { formatDate } from '../utils/dateUtils';

// Replace existing import line - find and update
// Then replace line 975:
{formatDate(booking.preferred_date)}